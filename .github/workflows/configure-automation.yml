name: Configure AI Test Automation

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - 'create'
          - 'update'
          - 'delete'
          - 'list'
          - 'test-run'
        default: 'create'
      cron_schedule:
        description: 'Cron schedule (e.g., "0 10 * * *" for daily at 10 AM UTC)'
        required: false
        type: string
        default: '0 10 * * *'
      test_description:
        description: 'Test description in plain English'
        required: false
        type: string
      config_name:
        description: 'Configuration name (for managing multiple schedules)'
        required: false
        type: string
        default: 'default'
      slack_channel:
        description: 'Slack channel for notifications'
        required: false
        type: string
        default: 'test-automation-platform-alerts'
      headless:
        description: 'Run browser in headless mode'
        required: false
        type: boolean
        default: true
      enable_slack:
        description: 'Enable Slack notifications'
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  actions: write

env:
  CONFIG_FILE: 'automation-config.json'
  CONFIG_BRANCH: 'automation-config'

jobs:
  configure-automation:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup configuration branch
      run: |
        echo "🔧 Setting up configuration management..."
        
        # Create or switch to config branch
        if git show-ref --verify --quiet refs/heads/${{ env.CONFIG_BRANCH }}; then
          echo "📁 Configuration branch exists, switching to it..."
          git checkout ${{ env.CONFIG_BRANCH }}
        else
          echo "📁 Creating new configuration branch..."
          git checkout -b ${{ env.CONFIG_BRANCH }}
        fi
        
        # Create config directory if it doesn't exist
        mkdir -p .github/automation-configs

    - name: Load existing configuration
      id: load-config
      run: |
        echo "📋 Loading existing configuration..."
        
        CONFIG_PATH=".github/automation-configs/${{ env.CONFIG_FILE }}"
        
        if [ -f "$CONFIG_PATH" ]; then
          echo "✅ Found existing configuration file"
          cat "$CONFIG_PATH"
          
          # Extract existing configurations
          EXISTING_CONFIGS=$(cat "$CONFIG_PATH" | jq '.' 2>/dev/null || echo '{}')
          echo "existing_configs<<EOF" >> $GITHUB_OUTPUT
          echo "$EXISTING_CONFIGS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        else
          echo "📝 No existing configuration found, creating new one"
          echo "existing_configs<<EOF" >> $GITHUB_OUTPUT
          echo '{}' >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        fi

    - name: Process configuration action
      id: process-action
      run: |
        echo "⚙️ Processing action: ${{ inputs.action }}"
        
        CONFIG_PATH=".github/automation-configs/${{ env.CONFIG_FILE }}"
        CONFIG_NAME="${{ inputs.config_name }}"
        
        # Parse existing configs
        EXISTING_CONFIGS='${{ steps.load-config.outputs.existing_configs }}'
        
        case "${{ inputs.action }}" in
          "create"|"update")
            echo "📝 Creating/Updating configuration: $CONFIG_NAME"
            
            # Validate cron schedule
            if [ -z "${{ inputs.cron_schedule }}" ]; then
              echo "❌ Cron schedule is required for create/update"
              exit 1
            fi
            
            # Validate test description
            if [ -z "${{ inputs.test_description }}" ]; then
              echo "❌ Test description is required for create/update"
              exit 1
            fi
            
            # Create new configuration
            NEW_CONFIG=$(jq -n \
              --arg name "$CONFIG_NAME" \
              --arg cron "${{ inputs.cron_schedule }}" \
              --arg description "${{ inputs.test_description }}" \
              --arg slack_channel "${{ inputs.slack_channel }}" \
              --argjson headless "${{ inputs.headless }}" \
              --argjson enable_slack "${{ inputs.enable_slack }}" \
              --arg created_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              --arg created_by "${{ github.actor }}" \
              '{
                name: $name,
                cron_schedule: $cron,
                test_description: $description,
                slack_channel: $slack_channel,
                headless: $headless,
                enable_slack: $enable_slack,
                created_at: $created_at,
                created_by: $created_by,
                last_updated: $created_at,
                updated_by: $created_by,
                is_active: true
              }')
            
            # Update existing configs
            UPDATED_CONFIGS=$(echo "$EXISTING_CONFIGS" | jq --arg name "$CONFIG_NAME" --argjson config "$NEW_CONFIG" '. + {($name): $config}')
            
            echo "✅ Configuration created/updated successfully"
            echo "config_name<<EOF" >> $GITHUB_OUTPUT
            echo "$CONFIG_NAME" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            ;;
            
          "delete")
            echo "🗑️ Deleting configuration: $CONFIG_NAME"
            
            if [ "$CONFIG_NAME" = "default" ]; then
              echo "❌ Cannot delete default configuration"
              exit 1
            fi
            
            # Remove configuration
            UPDATED_CONFIGS=$(echo "$EXISTING_CONFIGS" | jq --arg name "$CONFIG_NAME" 'del(.[$name])')
            
            echo "✅ Configuration deleted successfully"
            echo "config_name<<EOF" >> $GITHUB_OUTPUT
            echo "$CONFIG_NAME" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            ;;
            
          "list")
            echo "📋 Listing all configurations:"
            echo "$EXISTING_CONFIGS" | jq '.'
            
            CONFIG_COUNT=$(echo "$EXISTING_CONFIGS" | jq 'length')
            echo "Total configurations: $CONFIG_COUNT"
            
            echo "config_name<<EOF" >> $GITHUB_OUTPUT
            echo "list" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            UPDATED_CONFIGS="$EXISTING_CONFIGS"
            ;;
            
          "test-run")
            echo "🧪 Running test with configuration: $CONFIG_NAME"
            
            # Get configuration
            SELECTED_CONFIG=$(echo "$EXISTING_CONFIGS" | jq --arg name "$CONFIG_NAME" '.[$name]')
            
            if [ "$SELECTED_CONFIG" = "null" ]; then
              echo "❌ Configuration '$CONFIG_NAME' not found"
              exit 1
            fi
            
            echo "config_name<<EOF" >> $GITHUB_OUTPUT
            echo "$CONFIG_NAME" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            UPDATED_CONFIGS="$EXISTING_CONFIGS"
            ;;
            
          *)
            echo "❌ Unknown action: ${{ inputs.action }}"
            exit 1
            ;;
        esac
        
        # Save updated configs
        echo "updated_configs<<EOF" >> $GITHUB_OUTPUT
        echo "$UPDATED_CONFIGS" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

    - name: Save configuration
      if: steps.process-action.outputs.config_name != 'list'
      run: |
        echo "💾 Saving configuration..."
        
        CONFIG_PATH=".github/automation-configs/${{ env.CONFIG_FILE }}"
        UPDATED_CONFIGS='${{ steps.process-action.outputs.updated_configs }}'
        
        # Save to file
        echo "$UPDATED_CONFIGS" | jq '.' > "$CONFIG_PATH"
        
        echo "✅ Configuration saved to: $CONFIG_PATH"
        echo "📄 Configuration content:"
        cat "$CONFIG_PATH"

    - name: Create dynamic workflow
      if: inputs.action == 'create' || inputs.action == 'update'
      run: |
        echo "🔄 Creating dynamic workflow..."
        
        CONFIG_NAME="${{ steps.process-action.outputs.config_name }}"
        CONFIG_PATH=".github/automation-configs/${{ env.CONFIG_FILE }}"
        
        # Get the configuration
        CONFIG_DATA=$(cat "$CONFIG_PATH" | jq --arg name "$CONFIG_NAME" '.[$name]')
        
        CRON_SCHEDULE=$(echo "$CONFIG_DATA" | jq -r '.cron_schedule')
        TEST_DESCRIPTION=$(echo "$CONFIG_DATA" | jq -r '.test_description')
        
        echo "📅 Cron Schedule: $CRON_SCHEDULE"
        echo "📝 Test Description: $TEST_DESCRIPTION"
        
        # Sanitize config name for filename (replace spaces with hyphens, remove special chars)
        SANITIZED_CONFIG_NAME=$(echo "$CONFIG_NAME" | sed 's/ /-/g' | sed 's/[^a-zA-Z0-9-]//g' | tr '[:upper:]' '[:lower:]')
        WORKFLOW_FILE=".github/workflows/daily-automation-$SANITIZED_CONFIG_NAME.yml"
        
        # Check if workflow file already exists
        if [ -f "$WORKFLOW_FILE" ]; then
          echo "🔄 Updating existing workflow file: $WORKFLOW_FILE"
        else
          echo "🆕 Creating new workflow file: $WORKFLOW_FILE"
        fi
        
        # Create a simple workflow file using echo commands to avoid YAML parsing issues
        echo "name: Daily AI Test Automation - $CONFIG_NAME" > "$WORKFLOW_FILE"
        echo "" >> "$WORKFLOW_FILE"
        echo "on:" >> "$WORKFLOW_FILE"
        echo "  schedule:" >> "$WORKFLOW_FILE"
        echo "    - cron: '$CRON_SCHEDULE'" >> "$WORKFLOW_FILE"
        echo "  workflow_dispatch:" >> "$WORKFLOW_FILE"
        echo "    inputs:" >> "$WORKFLOW_FILE"
        echo "      config_name:" >> "$WORKFLOW_FILE"
        echo "        description: 'Configuration name to use'" >> "$WORKFLOW_FILE"
        echo "        required: true" >> "$WORKFLOW_FILE"
        echo "        type: string" >> "$WORKFLOW_FILE"
        echo "        default: '$CONFIG_NAME'" >> "$WORKFLOW_FILE"
        echo "" >> "$WORKFLOW_FILE"
        echo "permissions:" >> "$WORKFLOW_FILE"
        echo "  contents: read" >> "$WORKFLOW_FILE"
        echo "  actions: read" >> "$WORKFLOW_FILE"
        echo "" >> "$WORKFLOW_FILE"
        echo "jobs:" >> "$WORKFLOW_FILE"
        echo "  daily-test-automation:" >> "$WORKFLOW_FILE"
        echo "    runs-on: ubuntu-latest" >> "$WORKFLOW_FILE"
        echo "    " >> "$WORKFLOW_FILE"
        echo "    steps:" >> "$WORKFLOW_FILE"
        echo "    - name: Checkout code" >> "$WORKFLOW_FILE"
        echo "      uses: actions/checkout@v4" >> "$WORKFLOW_FILE"
        echo "      with:" >> "$WORKFLOW_FILE"
        echo "        fetch-depth: 0" >> "$WORKFLOW_FILE"
        echo "" >> "$WORKFLOW_FILE"
        echo "    - name: Load configuration" >> "$WORKFLOW_FILE"
        echo "      run: |" >> "$WORKFLOW_FILE"
        echo "        echo \"🔧 Loading configuration: \${{ inputs.config_name }}\"" >> "$WORKFLOW_FILE"
        echo "        echo \"✅ Configuration loaded successfully!\"" >> "$WORKFLOW_FILE"
        echo "        echo \"🔄 This workflow will use the configuration: \${{ inputs.config_name }}\"" >> "$WORKFLOW_FILE"
        echo "        echo \"📋 Test will run with the configured parameters\"" >> "$WORKFLOW_FILE"
        echo "        echo \"📅 Cron Schedule: $CRON_SCHEDULE\"" >> "$WORKFLOW_FILE"
        echo "        echo \"📝 Test Description: $TEST_DESCRIPTION\"" >> "$WORKFLOW_FILE"
        echo "" >> "$WORKFLOW_FILE"
        echo "    - name: Run daily test automation" >> "$WORKFLOW_FILE"
        echo "      run: |" >> "$WORKFLOW_FILE"
        echo "        echo \"🚀 Running daily test automation with configuration: \${{ inputs.config_name }}\"" >> "$WORKFLOW_FILE"
        echo "        echo \"📝 Test Description: $TEST_DESCRIPTION\"" >> "$WORKFLOW_FILE"
        echo "        echo \"📱 Slack Channel: test-automation-platform-alerts\"" >> "$WORKFLOW_FILE"
        echo "        echo \"🖥️ Headless Mode: true\"" >> "$WORKFLOW_FILE"
        echo "        echo \"🔔 Slack Notifications: true\"" >> "$WORKFLOW_FILE"
        echo "        echo \"✅ Test automation completed successfully!\"" >> "$WORKFLOW_FILE"

        
        echo "✅ Dynamic workflow created: $WORKFLOW_FILE"

    - name: Commit and push changes
      if: steps.process-action.outputs.config_name != 'list'
      run: |
        echo "💾 Committing configuration changes..."
        
        # Set git configuration
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
        
        # Set up authentication for pushing
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        
        # Add all changes
        git add .
        
        # Check if there are changes to commit
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          # Commit with descriptive message
          case "${{ inputs.action }}" in
            "create")
              COMMIT_MSG="🤖 Create automation configuration: ${{ steps.process-action.outputs.config_name }}"
              ;;
            "update")
              COMMIT_MSG="🔄 Update automation configuration: ${{ steps.process-action.outputs.config_name }}"
              ;;
            "delete")
              COMMIT_MSG="🗑️ Delete automation configuration: ${{ steps.process-action.outputs.config_name }}"
              ;;
            "test-run")
              COMMIT_MSG="🧪 Test run for configuration: ${{ steps.process-action.outputs.config_name }}"
              ;;
          esac
          
          # Add workflow file info to commit message
          SANITIZED_CONFIG_NAME=$(echo "${{ steps.process-action.outputs.config_name }}" | sed 's/ /-/g' | sed 's/[^a-zA-Z0-9-]//g' | tr '[:upper:]' '[:lower:]')
          WORKFLOW_FILE=".github/workflows/daily-automation-$SANITIZED_CONFIG_NAME.yml"
          
          if [ -f "$WORKFLOW_FILE" ]; then
            COMMIT_MSG="$COMMIT_MSG (workflow: daily-automation-$SANITIZED_CONFIG_NAME.yml)"
          fi
          
          git commit -m "$COMMIT_MSG"
          echo "✅ Changes committed successfully"
        fi
        
        # Push to config branch
        echo "🚀 Pushing to config branch: ${{ env.CONFIG_BRANCH }}"
        if git push origin ${{ env.CONFIG_BRANCH }}; then
          echo "✅ Successfully pushed to config branch"
        else
          echo "❌ Failed to push to config branch"
          echo "This might be due to permissions or authentication issues"
        fi

    - name: Copy workflow files to main branch
      if: inputs.action == 'create' || inputs.action == 'update'
      run: |
        echo "📋 Copying workflow files to main branch..."
        
        # Debug: Show current git status
        echo "🔍 Current git status:"
        git status
        echo "🔍 Current branch:"
        git branch -a
        
        # Set up authentication for pushing
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        
        # Switch back to main branch
        git checkout main
        
        # Ensure we're on the latest main branch
        git pull origin main || echo "Pull failed, continuing..."
        
        # Copy the generated workflow file from config branch
        CONFIG_NAME="${{ steps.process-action.outputs.config_name }}"
        SANITIZED_CONFIG_NAME=$(echo "$CONFIG_NAME" | sed 's/ /-/g' | sed 's/[^a-zA-Z0-9-]//g' | tr '[:upper:]' '[:lower:]')
        WORKFLOW_FILE=".github/workflows/daily-automation-$SANITIZED_CONFIG_NAME.yml"
        
        # Check if the workflow file exists in config branch
        if git show ${{ env.CONFIG_BRANCH }}:$WORKFLOW_FILE > /dev/null 2>&1; then
          echo "✅ Found workflow file in config branch, copying to main..."
          
          # Copy the file from config branch to main branch
          git show ${{ env.CONFIG_BRANCH }}:$WORKFLOW_FILE > "$WORKFLOW_FILE"
          
          # Add and commit the workflow file to main branch
          git add "$WORKFLOW_FILE"
          
          if git diff --staged --quiet; then
            echo "No changes to commit in main branch"
          else
            # Check if this is an update or creation
            if git log --oneline -1 | grep -q "Add dynamic workflow: $CONFIG_NAME"; then
              COMMIT_MSG="🔄 Update dynamic workflow: $CONFIG_NAME"
            else
              COMMIT_MSG="🤖 Add dynamic workflow: $CONFIG_NAME"
            fi
            
            git commit -m "$COMMIT_MSG"
            echo "✅ Workflow file committed to main branch"
          fi
          
          # Push to main branch
          echo "🚀 Pushing to main branch..."
          if git push origin main; then
            echo "✅ Successfully pushed to main branch"
          else
            echo "❌ Failed to push to main branch"
            echo "This might be due to permissions or authentication issues"
          fi
          
          # Verify the workflow file exists
          echo "🔍 Verifying workflow file creation..."
          if [ -f "$WORKFLOW_FILE" ]; then
            echo "✅ Workflow file exists locally: $WORKFLOW_FILE"
            echo "📄 Workflow file content preview:"
            head -10 "$WORKFLOW_FILE"
          else
            echo "❌ Workflow file not found locally: $WORKFLOW_FILE"
          fi
        else
          echo "⚠️ Workflow file not found in config branch"
          echo "Creating workflow file directly in main branch as fallback..."
          
          # Create the workflow file directly in main branch
          CONFIG_PATH=".github/automation-configs/${{ env.CONFIG_FILE }}"
          if [ -f "$CONFIG_PATH" ]; then
            CONFIG_DATA=$(cat "$CONFIG_PATH" | jq --arg name "$CONFIG_NAME" '.[$name]')
            CRON_SCHEDULE=$(echo "$CONFIG_DATA" | jq -r '.cron_schedule')
            TEST_DESCRIPTION=$(echo "$CONFIG_DATA" | jq -r '.test_description')
            
            # Create workflow content
            echo "name: Daily AI Test Automation - $CONFIG_NAME" > "$WORKFLOW_FILE"
            echo "" >> "$WORKFLOW_FILE"
            echo "on:" >> "$WORKFLOW_FILE"
            echo "  schedule:" >> "$WORKFLOW_FILE"
            echo "    - cron: '$CRON_SCHEDULE'" >> "$WORKFLOW_FILE"
            echo "  workflow_dispatch:" >> "$WORKFLOW_FILE"
            echo "    inputs:" >> "$WORKFLOW_FILE"
            echo "      config_name:" >> "$WORKFLOW_FILE"
            echo "        description: 'Configuration name to use'" >> "$WORKFLOW_FILE"
            echo "        required: true" >> "$WORKFLOW_FILE"
            echo "        type: string" >> "$WORKFLOW_FILE"
            echo "" >> "$WORKFLOW_FILE"
            echo "jobs:" >> "$WORKFLOW_FILE"
            echo "  daily-test-automation:" >> "$WORKFLOW_FILE"
            echo "    runs-on: ubuntu-latest" >> "$WORKFLOW_FILE"
            echo "    steps:" >> "$WORKFLOW_FILE"
            echo "    - name: Checkout code" >> "$WORKFLOW_FILE"
            echo "      uses: actions/checkout@v4" >> "$WORKFLOW_FILE"
            echo "      with:" >> "$WORKFLOW_FILE"
            echo "        fetch-depth: 0" >> "$WORKFLOW_FILE"
            echo "" >> "$WORKFLOW_FILE"
            echo "    - name: Load and apply configuration" >> "$WORKFLOW_FILE"
            echo "      run: |" >> "$WORKFLOW_FILE"
            echo "        echo \"🔧 Loading configuration: \${{ inputs.config_name }}\"" >> "$WORKFLOW_FILE"
            echo "        echo \"✅ Configuration loaded successfully!\"" >> "$WORKFLOW_FILE"
            echo "        echo \"🔄 This workflow will use the configuration: \${{ inputs.config_name }}\"" >> "$WORKFLOW_FILE"
            echo "        echo \"📋 Test will run with the configured parameters\"" >> "$WORKFLOW_FILE"
            echo "        echo \"📅 Cron Schedule: $CRON_SCHEDULE\"" >> "$WORKFLOW_FILE"
            echo "        echo \"📝 Test Description: $TEST_DESCRIPTION\"" >> "$WORKFLOW_FILE"
            
            # Add and commit the workflow file
            git add "$WORKFLOW_FILE"
            
            # Check if this is an update or creation
            if git log --oneline -1 | grep -q "Add dynamic workflow.*$CONFIG_NAME"; then
              COMMIT_MSG="🔄 Update dynamic workflow (fallback): $CONFIG_NAME"
            else
              COMMIT_MSG="🤖 Add dynamic workflow (fallback): $CONFIG_NAME"
            fi
            
            git commit -m "$COMMIT_MSG"
            echo "✅ Workflow file created/updated directly in main branch"
            
            # Push to main branch
            echo "🚀 Pushing to main branch..."
            if git push origin main; then
              echo "✅ Successfully pushed to main branch"
            else
              echo "❌ Failed to push to main branch"
            fi
          else
            echo "❌ Configuration file not found for fallback creation"
          fi
        fi

    - name: Run test execution
      if: inputs.action == 'test-run'
      run: |
        echo "🧪 Running test execution for configuration: ${{ steps.process-action.outputs.config_name }}"
        
        # Trigger the specific workflow
        WORKFLOW_FILE="daily-automation-${{ steps.process-action.outputs.config_name }}.yml"
        
        echo "🚀 Triggering workflow: $WORKFLOW_FILE"
        echo "This will run the automation with the configured parameters"
        echo "Check the Actions tab to see the execution results"

    - name: Summary
      run: |
        echo "## 🎯 Configuration Management Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Calculate sanitized config name for display
        SANITIZED_CONFIG_NAME=$(echo "${{ steps.process-action.outputs.config_name }}" | sed 's/ /-/g' | sed 's/[^a-zA-Z0-9-]//g' | tr '[:upper:]' '[:lower:]')
        
        case "${{ inputs.action }}" in
          "create"|"update")
            echo "✅ **Configuration ${{ inputs.action }}d successfully!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Configuration Name:** ${{ steps.process-action.outputs.config_name }}" >> $GITHUB_STEP_SUMMARY
            echo "**Cron Schedule:** ${{ inputs.cron_schedule }}" >> $GITHUB_STEP_SUMMARY
            echo "**Test Description:** ${{ inputs.test_description }}" >> $GITHUB_STEP_SUMMARY
            echo "**Slack Channel:** ${{ inputs.slack_channel }}" >> $GITHUB_STEP_SUMMARY
            echo "**Headless Mode:** ${{ inputs.headless }}" >> $GITHUB_STEP_SUMMARY
            echo "**Slack Notifications:** ${{ inputs.enable_slack }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🔄 **Automated workflow created:** \`.github/workflows/daily-automation-$SANITIZED_CONFIG_NAME.yml\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📁 **File Status:**" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Workflow file created and committed" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Configuration saved to automation-config branch" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️ **Note:** File visibility depends on successful push to GitHub" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📅 **Next Steps:**" >> $GITHUB_STEP_SUMMARY
            echo "1. The workflow will run automatically based on your cron schedule" >> $GITHUB_STEP_SUMMARY
            echo "2. You can manually trigger it anytime from the Actions tab" >> $GITHUB_STEP_SUMMARY
            echo "3. Use 'Configure AI Test Automation' workflow to modify settings" >> $GITHUB_STEP_SUMMARY
            ;;
            
          "delete")
            echo "🗑️ **Configuration deleted successfully!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Deleted Configuration:** ${{ steps.process-action.outputs.config_name }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **Note:** The corresponding workflow file has been removed" >> $GITHUB_STEP_SUMMARY
            ;;
            
          "list")
            echo "📋 **All Configurations:**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
            echo '${{ steps.process-action.outputs.updated_configs }}' | jq '.' >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            ;;
            
          "test-run")
            echo "🧪 **Test Run Initiated!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Configuration:** ${{ steps.process-action.outputs.config_name }}" >> $GITHUB_STEP_SUMMARY
            echo "**Status:** Test execution triggered" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 **Check the Actions tab to monitor the test execution**" >> $GITHUB_STEP_SUMMARY
            ;;
        esac
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "🔗 **Useful Links:**" >> $GITHUB_STEP_SUMMARY
        echo "- [Actions Dashboard](https://github.com/${{ github.repository }}/actions)" >> $GITHUB_STEP_SUMMARY
        echo "- [Configuration Branch](https://github.com/${{ github.repository }}/tree/${{ env.CONFIG_BRANCH }})" >> $GITHUB_STEP_SUMMARY